#!/bin/bash
# List of options used exclusively in dab, but imports abs options too
# https://keyaedisa.github.io, https://github.com/keyaedisa

source /etc/dab/options/.options

_buildAndPushToRepo() {
 source /etc/dab/dev/buildAndPushToRepo
}

_mergeDevToProd() {
 source /etc/dab/dev/mergeDevToProd
}

_push2Git() {
 source /etc/dab/dev/push2Git
}

_help() {
 source /etc/dab/dev/help
 }
