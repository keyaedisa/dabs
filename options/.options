#!/bin/bash
# https://keyaedisa.github.io/

currentVersion() {
 source /etc/dab/options/currentVersion
}

custom() {
 source /etc/dab/options/custom
}

helpOpt() {
 source /etc/dab/options/help
}

vanilla() {
 source /etc/dab/options/vanilla
}

vanillaBaseline() {
 source /etc/dab/options/vanillaBaseline
}

xero() {
 source /etc/dab/options/xero
}

xerog() {
 source /etc/dab/options/xerog
}

