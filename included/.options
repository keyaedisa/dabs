#!/bin/bash

_buildAndPushToRepo(){
 source /etc/${app_name}/included/buildAndPushToRepo
}

_pushToGit(){
 source /etc/${app_name}/included/push2Git
}

_mergeDevToProd(){
 source /etc/${app_name}/included/mergeDevToProd
}
